export interface MediaReport {
  title: string;
  source: string;
  date: string;
  url: string;
}

/**
 * Real press mentions of Shikshadwar Foundation. Add entries here as
 * coverage comes in — the Media Reports page shows an empty state when
 * this list is empty.
 */
export const mediaReports: MediaReport[] = [];
